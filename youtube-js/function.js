// NB: A function is simply a set of statment that performs a task or runs a calculation.

// Peforming a task
function greet(names, LastName) {
    console.log(`Hello ${names} ${LastName}`);
}

greet("John", "Senario");


// Calculating a value..

function square(number) {
    return number * number;
}

console.log(square(2));